CREATE DATABASE FeuillesDB
go

use FeuillesDB
go


CREATE TABLE Feuilles
(
nume_annee_numms varchar(500) PRIMARY KEY,
numero_employe varchar(500),
annee varchar(500),
numero_semaine varchar(500),
jour1 varchar(500),
jour2 varchar(500),
jour3 varchar(500),
jour4 varchar(500),
jour5 varchar(500),
weekend1 varchar(500),
weekend2 varchar(500)
);
